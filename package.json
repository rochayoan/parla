{
  "name": "parla",
  "private": true,
  "scripts": {
    "backend:dev": "cd backend && npm run dev",
    "desktop:dev": "cd apps/desktop && npm run dev",
    "dev": "concurrently \"npm run backend:dev\" \"npm run desktop:dev\""
  },
  "devDependencies": {
    "concurrently": "^9.2.3"
  }
}
